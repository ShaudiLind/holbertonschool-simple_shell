#include <string.h>
#include <stdlib.h>

/* ... inside your main shell loop ... */

/* Assuming 'args' is the array of strings containing the command */
if (strcmp(args[0], "exit") == 0)
{
  /* 
   * IMPORTANT: You must free all allocated memory here!
   * If you have allocated space for the input line, the argument array, 
   * or any paths found during your 0.3 task, free them now.
   */
  free(line); 
  free(args);

  /* Terminate the shell */
  exit(0);
}

